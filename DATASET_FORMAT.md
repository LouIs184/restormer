# 双能相位CT图像重建数据集格式说明

## 目录结构

```
联影双能相位数据2080/
├── 11034_307811_960+_AXIAL_CE1_M067Y_20211215_Thick1_Incre1/
│   ├── rec_no_pbi.raw
│   ├── rec_pbi_fs_dec_blur_phase_100000.raw
│   ├── proj_no_pbi.raw
│   └── proj_pbi_fs_dec_blur_phase_100000.raw
├── 11849_308361_960+_AXIAL_CE1_M044Y_20211216_Thick1_Incre1/
│   ├── rec_no_pbi.raw
│   ├── rec_pbi_fs_dec_blur_phase_100000.raw
│   ├── proj_no_pbi.raw
│   └── proj_pbi_fs_dec_blur_phase_100000.raw
├── ... (共13个病人文件夹)
```

每个病人文件夹包含 **四个 .raw 文件**，分为两类：

- **重建图像（reconstruction）**：形状 `(512, 512, 160)` 的三维体数据，float32 类型，小端序
- **投影数据（projection）**：形状 `(720, 128, 512)` 的三维投影数据，float32 类型，小端序

---

## 一、重建图像文件 (Reconstruction)

### 文件说明

| 文件名 | 语义 | 用途 |
|--------|------|------|
| `rec_no_pbi.raw` | Ground Truth（无相位束硬化校正的重建结果） | 训练目标 / 标签 |
| `rec_pbi_fs_dec_blur_phase_100000.raw` | 模糊相位校正后的重建结果 | 模型输入 |

- **存储格式**：raw 二进制，无头（no header），按 **Z-axis fastest** 顺序存储，即切片顺序为 `z=0,1,...,159`，每张切片尺寸为 `512 × 512`。
- **数据类型**：32-bit 浮点数（`np.float32`），小端字节序。
- **数据范围**：**HU 值（Hounsfield Unit，亨氏单位）**，即标准 CT 值。空气约为 -1000 HU，水约为 0 HU，骨骼约为 +400~+1000 HU。具体数值范围因病例和扫描参数而异，建议在训练时根据数据集统计做归一化。

### 读取方式

```python
import numpy as np

data = np.fromfile("rec_no_pbi.raw", dtype=np.float32).reshape(160, 512, 512)
# 此时 data.shape = (160, 512, 512)，data[0] 为第一张切片

# 如需 (512, 512, 160) 排列：
data = data.transpose(1, 2, 0)  # shape: (512, 512, 160)
```

### ⚠️ 首尾切片无信号

由于扫描和处理中的边界效应，**160 张切片中的第 0 张（第一张）和第 159 张（最后一张）几乎不包含有效信号**，在训练时应当剔除。

标准做法是用 `data[1:-1]` 取中间 158 张切片：

```python
data = np.fromfile("rec_no_pbi.raw", dtype=np.float32).reshape(160, 512, 512)
valid_slices = data[1:-1]  # shape: (158, 512, 512)，去掉首尾
```

---

## 二、投影数据文件 (Projection)

### 文件说明

| 文件名 | 语义 | 用途 |
|--------|------|------|
| `proj_pbi_fs_dec_blur_phase_100000.raw` | 模糊相位校正对应的投影数据（含束硬化伪影） | **模型输入**（与 `rec_pbi_fs_dec_blur_phase_100000.raw` 对应） |
| `proj_no_pbi.raw` | 无相位束硬化校正的投影数据（干净） | **训练目标 / 标签**（与 `rec_no_pbi.raw` 对应） |

- **存储格式**：raw 二进制，无头（no header），按 **视角为块连续存储**，即先存第 0 视角的全部 128×512 个值，再存第 1 视角，以此类推。各个视角内部按探测器行优先（row-major）排列。
- **数据类型**：32-bit 浮点数（`np.float32`），小端字节序。
- **数据范围**：**衰减对数投影（post-log 线积分）**，即对探测器透射强度取负对数后的值，物理上正比于线积分 `μ·L`（可能经过缩放/归一化，非标准物理单位）。典型特征：**空气处值 ≈ 0，穿物体处值大**（路径越长 / 越致密，值越大）；若是 pre-log 透射强度，空气应为最大值。实测范围约 `[-0.066, 0.276]`，存在少量负值（来自校准偏移、束硬化校正或双能分解等后处理残留），属正常。

### 数据形状

```
(720, 128, 512)
  │    │    └── 探测器通道数（detector channels，即探测器宽度方向像素数）
  │    └── 探测器行数（detector rows，即探测器高度方向像素数）
  └── 视角数（number of projection views / angles）
```

三个维度的含义：

| 维度 | 大小 | 物理含义 |
|------|------|----------|
| 视角 (view / angle) | 720 | 旋转一周的投影视角数（等角度间隔采样） |
| 探测器行 (detector row) | 128 | 沿探测器高度方向（Z方向）的采样行数 |
| 探测器通道 (detector channel) | 512 | 沿探测器宽度方向的采样通道数 |

### 读取方式

```python
import numpy as np

# 直接 reshape 为 (720, 128, 512)，即 (views, rows, channels)
proj = np.fromfile("proj_no_pbi.raw", dtype=np.float32).reshape(720, 128, 512)
# proj.shape = (720, 128, 512)
# proj[0] 为第一个视角的投影（二维探测器平面，shape (128, 512)）
```

如需重排为其他维度顺序：

```python
# 重排为 (views, channels, rows)
proj_views_first = proj.transpose(0, 2, 1)  # shape: (720, 512, 128)
```

### ⚠️ 注意事项

1. **投影数据与重建图像严格对应**：`proj_pbi_fs_dec_blur_phase_100000.raw`（输入）对应 `rec_pbi_fs_dec_blur_phase_100000.raw`，`proj_no_pbi.raw`（目标）对应 `rec_no_pbi.raw`。两者在视角维度上完全对齐，可用于投影域到图像域的端到端学习。
2. **首尾视角可能存在边界效应**：与重建图像类似，投影数据的首尾视角（第 0 和 第 719 个视角）可能因扫描起始/结束阶段的机械运动而包含伪影，使用时建议检查后酌情剔除。
3. **探测器坐标约定**：探测器行维（dim 1，大小 128）对应探测器高度方向（Z 轴方向）；探测器通道维（dim 2，大小 512）对应探测器宽度方向（扇束/锥束的横向）。此排列方式与常见的 CT 投影数据存储约定一致。
4. **post-log 域，伪影可加**：数据为衰减对数投影（线积分域），因此 `proj_pbi_...`（含伪影）与 `proj_no_pbi`（干净）之差直接等于伪影的线积分增量，在数学上是**可加**的。对采用残差结构（`output = input + residual`，如 `DenseUNet2d.forward`）的模型，残差即对应伪影 sinogram，物理意义成立。

---

## 三、数据配对关系

每个文件夹内四个 .raw 文件构成两组**输入-目标对**：

| 数据域 | 输入（含伪影） | 目标（干净） |
|--------|---------------|-------------|
| 重建图像 (Reconstruction) | `rec_pbi_fs_dec_blur_phase_100000.raw` | `rec_no_pbi.raw` |
| 投影数据 (Projection) | `proj_pbi_fs_dec_blur_phase_100000.raw` | `proj_no_pbi.raw` |

**对应关系说明**：

- **图像域**：`rec_pbi_fs_dec_blur_phase_100000.raw` 的第 i 张切片 ↔ `rec_no_pbi.raw` 的第 i 张切片 —— 逐切片配对的输入-目标重建图像对
- **投影域**：`proj_pbi_fs_dec_blur_phase_100000.raw` ↔ `proj_no_pbi.raw` —— 逐视角配对的输入-目标投影数据对
- **跨域**：`proj_pbi_fs_dec_blur_phase_100000.raw` ↔ `rec_pbi_fs_dec_blur_phase_100000.raw` 来自同一原始扫描，几何参数、病人位置和扫描协议完全一致；`proj_no_pbi.raw` ↔ `rec_no_pbi.raw` 同理

**投影与重建并非直接逐像素对应**（投影需经过重建算法才能得到重建图像），但两者来自同一原始扫描，空间位置完全对齐。

### 使用场景说明

根据训练任务不同，可选择不同的输入-目标组合：

| 任务 | 输入 | 目标 |
|------|------|------|
| 图像域去伪影 (传统方案) | `rec_pbi_fs_dec_blur_phase_100000.raw` | `rec_no_pbi.raw` |
| 投影域去伪影 | `proj_pbi_fs_dec_blur_phase_100000.raw` | `proj_no_pbi.raw` |
| 双域联合 / 端到端学习 | 上述两者之一或组合 | 对应的目标 |

---

## 四、病人命名规范

文件夹名编码了病人信息，格式如下：

```
{patientID}_{studyID}_960+_AXIAL_CE{phase}_M|F{age}Y_{date}_Thick1_Incre1
```

| 字段 | 含义 | 示例 |
|------|------|------|
| `patientID` | 病人编号 | `11034` |
| `studyID` | 检查序列号 | `307811` |
| `960+` | 扫描协议（960+ 视野） | 固定 |
| `AXIAL` | 轴向扫描 | 固定 |
| `CE{0\|1}` | 是否增强（0=平扫, 1=增强） | `CE1` |
| `M\|F` | 性别（M=男, F=女） | `M` |
| `{age}Y` | 年龄 | `067Y` |
| `{date}` | 检查日期 (YYYYMMDD) | `20211215` |
| `Thick1` | 层厚 1mm | 固定 |
| `Incre1` | 层间距 1mm | 固定 |

### 病人统计

| 序号 | 病人目录 | 性别 | 年龄 | 增强 | 检查日期 |
|------|----------|------|------|------|----------|
| 1 | 11034_307811_... | M | 67 | 增强 | 2021-12-15 |
| 2 | 11849_308361_... | M | 44 | 增强 | 2021-12-16 |
| 3 | 15962_15633_... | M | 34 | 增强 | 2020-06-01 |
| 4 | 15962_35868_... | M | 34 | 增强 | 2020-06-01 |
| 5 | 17238_33314_... | F | 63 | 增强 | 2022-01-06 |
| 6 | 18537_93073_... | M | 67 | **平扫** | 2021-04-30 |
| 7 | 18631_303226_... | F | 58 | 增强 | 2021-12-16 |
| 8 | 38423_35948_... | M | 49 | 增强 | 2020-06-01 |
| 9 | 51741_308269_... | M | 48 | 增强 | 2021-12-15 |
| 10 | 51789_393117_... | M | 74 | 增强 | 2021-12-15 |
| 11 | 72278_406010_... | F | 71 | 增强 | 2021-12-16 |
| 12 | 91963_301643_... | M | 70 | 增强 | 2021-12-16 |
| 13 | 91963_53624_... | M | 70 | 增强 | 2021-12-16 |

- 病人 `15962` 和 `91963` 各有两次独立检查（不同 studyID）。
- 总共 13 个文件夹 / 13 次扫描，覆盖 11 位病人。

---

## 五、数据量

- 每个重建图像 `.raw` 文件：**167,772,160 字节** (≈160 MB) —— `512 × 512 × 160 × 4 bytes`
- 每个投影数据 `.raw` 文件：**188,743,680 字节** (≈180 MB) —— `720 × 128 × 512 × 4 bytes`
- 每个病人文件夹：**≈680 MB**（2 个重建文件 × 160 MB + 2 个投影文件 × 180 MB）
- 数据集总计：**≈8.84 GB**（13 个文件夹 × 680 MB）

---

## 六、训练/验证集划分建议

建议按病人划分，避免同一病人的不同扫描同时出现在训练集和验证集中。例如：

- 训练集：病人 11034, 11849, 15962(两次), 17238, 18631, 38423, 51741, 51789, 72278, 91963(两次) —— 共 11 次扫描
- 验证集：病人 18537 —— 共 1 次扫描

---

## 附录：快速读取示例

一次性加载单个病人的全部四种数据：

```python
import numpy as np

patient_dir = "11034_307811_960+_AXIAL_CE1_M067Y_20211215_Thick1_Incre1"
base = f"data/{patient_dir}"

# ---- 重建图像（图像域） ----
# 输入：模糊相位校正结果（含伪影）
rec_input = np.fromfile(f"{base}/rec_pbi_fs_dec_blur_phase_100000.raw", dtype=np.float32).reshape(160, 512, 512)[1:-1]  # (158, 512, 512)
# 目标：无相位束硬化校正结果（干净）
rec_target = np.fromfile(f"{base}/rec_no_pbi.raw", dtype=np.float32).reshape(160, 512, 512)[1:-1]                     # (158, 512, 512)

# ---- 投影数据（投影域） ----
# 输入：模糊相位校正对应的投影（含伪影）
proj_input = np.fromfile(f"{base}/proj_pbi_fs_dec_blur_phase_100000.raw", dtype=np.float32).reshape(720, 128, 512)    # (720, 128, 512)
# 目标：无相位束硬化校正对应的投影（干净）
proj_target = np.fromfile(f"{base}/proj_no_pbi.raw", dtype=np.float32).reshape(720, 128, 512)                        # (720, 128, 512)

print(f"重建图像 — 输入: {rec_input.shape}, 目标: {rec_target.shape}")
print(f"投影数据 — 输入: {proj_input.shape}, 目标: {proj_target.shape}")
```
